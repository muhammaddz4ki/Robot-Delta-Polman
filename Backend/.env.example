# Server Configuration
PORT=5000

# Database Configuration (MySQL / MariaDB)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=robot_delta_db

# Security & Authentication
JWT_SECRET=delta_robot_super_secret_key_change_me_in_production
